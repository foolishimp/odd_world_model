# Common Schemas

This directory holds the canonical JSON schema documents for the first common
world-model representation tenant.

These schemas define the shared carrier law for:

- world fragments
- world-model objects
- Markov objects
- treatment surfaces
- covariance edges
- adjoint mappings
- temporal reference artifacts
- projection specifications

They are shared realization surfaces. They do not replace the constitutional
truth in `specification/`.
