# Generated Test Stack Profile

This test stack profile is regenerated by the bounded odd_sdlc constructor turn.

**Project**: odd_world_model
**Platform**: Python / ODD_METHOD / GTL + ABG
**Generated**: 2026-04-16
**Derived from**: `build_tenants/python/design/40-generated-test-design.md`, `build_tenants/python/design/40-generated-implementation-design.md`, `build_tenants/python/design/40-generated-implementation-stack.md`
**Status**: Active selected test stack for the current Python tenant

---

## 1. Selected Stack

- selected harness family: `python_pytest`
- selected stack binding: `build_tenants_python_test_env`
- primary harness: `pytest`
- governed code root: `build_tenants/python/code/odd_world_model`
- governed test root: `build_tenants/python/test_env/tests`
- support root: `build_tenants/python/test_env`
- report root: `build_tenants/python/test_env/reports`
- runtime invocation model: `PYTHONPATH=.genesis:.odd_sdlc/python/code` for dogfood governance and `PYTHONPATH=build_tenants/python/code` or installed `.odd_world_model/python/code` for direct odd_world_model execution as appropriate

---

## 2. Stack Rationale

This stack matches the current implemented line:

- the active odd_world_model carrier is a Python package
- the current proving corpus is already exercised through Python entry points
- deterministic qualification is already available through Python CLI checks and `py_compile`
- `pytest` is the lightest lawful harness for unit, integration, and installed-sandbox tests in the same tenant

No parallel JS/TS, JVM, or DB-native test harness is introduced in this wave.

---

## 3. Planned Execution Lanes

### Lane A — Fast Deterministic Qualification

Commands in this lane include:

- `python -m py_compile ...`
- installed `odd_sdlc` deterministic FD checks over the mutable odd_world_model workspace
- targeted `pytest` runtime/catalog/constructor assertions

### Lane B — Source Workspace Steel-Thread Qualification

Commands in this lane include:

- `python -m odd_world_model.build_line.fpml_trade_domain`
- `python -m odd_world_model.build_line.trade_to_apra`
- `python -m odd_world_model.query.trade_to_apra`
- `python -m odd_world_model.proof.trade_to_apra`
- `pytest` integration assertions over the retained corpus outputs

### Lane C — Installed Sandbox Qualification

Commands in this lane include:

- install odd_world_model into a clean sandbox workspace
- run installed odd_world_model CLI/operator surfaces
- assert the retained trade/APRA scenario converges from the installed product boundary

---

## 4. Report And Evidence Model

- `pytest` should emit machine-readable evidence into `build_tenants/python/test_env/reports/`
- a JUnit-style XML report is preferred for stable archive summarization
- human-readable command summaries may be written alongside the XML report when helpful
- the generated run archive should summarize:
  - commands run
  - report files produced
  - total tests, failures, and errors
  - whether the source workspace steel thread converged
  - whether the installed sandbox proof converged

---

## 5. Constraints

- realized test files must carry `Validates:` tags for the requirements claimed by testcase authority
- the governed test root must stay under `build_tenants/python/test_env/tests`
- installed-sandbox proof remains part of the active stack, not a future optional add-on
- query/proof assertions remain downstream of the published semantic surfaces and do not become a parallel source of truth

---

## 6. Immediate Downstream Use

This stack profile is the authority for:

- generating governed test modules
- selecting the first realized pytest test files
- shaping the first run archive and testcase-authority surfaces
