# Generated Test Design

> Stale reference: this file is retained as Python-era test-design evidence.
> Current target test and proof design lives in
> `build_tenants/typescript/design/50-test-and-proof-design.md`.

This test design surface is regenerated by the bounded odd_sdlc constructor turn.

**Project**: odd_world_model
**Platform**: Python / ODD_METHOD / GTL + ABG
**Generated**: 2026-04-16
**Derived from**: `build_tenants/python/design/30-generated-odd-design.md`, `specification/scenarios/40-generated-scenarios.md`, `build_tenants/python/design/40-generated-implementation-design.md`, `build_tenants/python/design/40-generated-implementation-modules.md`
**Status**: Stale reference test-design surface for the old Python tenant

---

## 1. Governed Qualification Boundary

- qualification work is tied to the governed implementation branch under `build_tenants/python/`, not a shadow proving subset
- generated test files must live under the governed Python test surface and carry explicit `Validates:` tags for the requirements they claim
- archived run evidence must summarize execution over the current odd_world_model package and retained proving corpus
- the test line must prove both software-delivery governance and the semantic world-model build/composition line

---

## 2. Test Objective

The current test line must prove that the active Python tenant can:

1. expose a lawful ODD carrier over GTL + ABG
2. recover trace, assurance, attribute-ledger, object-cut, publication, and composition surfaces from the retained proving corpus
3. answer filesystem-first explainability queries over the published/composed outputs
4. install the released product into a sandbox workspace and run the retained two-domain scenario through that installed surface

The test line therefore has two coupled obligations:

- **carrier qualification**
  prove the runtime, catalog, constructor, and self-test surfaces
- **semantic steel-thread qualification**
  prove the retained trade and APRA corpus from source to query/proof outputs

---

## 3. Governed Test Families

### 3.1 Carrier Runtime Qualification

Targets:

- `odd_world_model.__main__`
- `odd_world_model.app`
- `odd_world_model.function_catalog`
- `odd_world_model.gtl_module`
- `odd_world_model.workspace_assets`
- `odd_world_model.constructor`
- `odd_world_model.self_test`

Purpose:

- prove the published ODD carrier remains callable
- prove the active executive program and graph-function catalog are stable
- prove constructor dispatch and result ingestion can converge a clean workspace

Primary scenario linkage:

- `SCN-DOGFOOD-001`

### 3.2 World-Model Build-Line Qualification

Targets:

- `odd_world_model.adapters.fpml_confirmation`
- `odd_world_model.build_line.fpml_trade_domain`
- `odd_world_model.build_line.trade_to_apra`
- `odd_world_model.world_model.materialize`
- `odd_world_model.world_model.load`
- `odd_world_model.world_model.validate`

Purpose:

- prove the retained semantic chain
- prove trace and assurance surfaces are materially produced
- prove the attribute ledger remains the immediate semantic source of object cuts
- prove bounded publication and composition remain stable

Primary scenario linkage:

- `SCN-BUILD-001`
- `SCN-BUILD-002`
- `SCN-BUILD-003`
- `SCN-COMP-001`

### 3.3 Query And Explainability Qualification

Targets:

- `odd_world_model.query.domain`
- `odd_world_model.query.trade_to_apra`
- `odd_world_model.query_contract`
- supporting published query artifacts under the sandbox example root

Purpose:

- prove the current query plane remains downstream of published artifacts
- prove reverse recoverability from APRA-side outputs back through object cut, attribute ledger, assurance, trace, and source evidence

Primary scenario linkage:

- `SCN-QUERY-001`
- `SCN-PROOF-001`

### 3.4 Installed Product Qualification

Targets:

- `odd_world_model.release.install`
- installed CLI entry points in a clean sandbox workspace

Purpose:

- prove released-product installation remains distinct from the mutable source project
- prove the installed product can run the retained two-domain scenario in a sandbox workspace

Primary scenario linkage:

- `SCN-DOGFOOD-001`
- `SCN-PROOF-001`

---

## 4. Test Lanes

### Lane A — Fast Deterministic Qualification

This lane covers:

- `py_compile`
- deterministic FD checks
- unit-style Python assertions over catalog/runtime/helpers

This is the cheapest lane and should run whenever the code surface changes.

### Lane B — Steel-Thread Corpus Qualification

This lane covers:

- `odd_world_model.build_line.fpml_trade_domain`
- `odd_world_model.build_line.trade_to_apra`
- `odd_world_model.query.trade_to_apra`
- `odd_world_model.proof.trade_to_apra`
- schema validation over the produced review/published surfaces

This lane proves the retained semantic chain in the source workspace.

### Lane C — Installed Sandbox Qualification

This lane covers:

- installing the released odd_world_model product into a clean sandbox workspace
- running the installed CLI/operator surfaces
- proving the retained two-domain scenario from the installed product boundary

This is the live installed-product proof line.

---

## 5. Requirement And Scenario Allocation

### Carrier Requirements

- `REQ-ODD-WORLD-MODEL-ODD-CARRIER-001`
- `REQ-ODD-WORLD-MODEL-ODD-CARRIER-002`
- `REQ-ODD-WORLD-MODEL-ODD-CARRIER-003`
- `REQ-ODD-WORLD-MODEL-ODD-CARRIER-004`
- `REQ-ODD-WORLD-MODEL-ODD-CARRIER-005`
- `REQ-ODD-WORLD-MODEL-ODD-CARRIER-006`

These are primarily qualified by Lane A and Lane C.

### Build And World-Object Requirements

- `REQ-ODD-WORLD-MODEL-BUILD-CAP-001` through `REQ-ODD-WORLD-MODEL-BUILD-CAP-006`
- `REQ-ODD-WORLD-MODEL-BUILD-CONSTRAINT-001` through `REQ-ODD-WORLD-MODEL-BUILD-CONSTRAINT-005`
- `REQ-ODD-WORLD-MODEL-WORLD-OBJECT-001` through `REQ-ODD-WORLD-MODEL-WORLD-OBJECT-012`

These are primarily qualified by Lane B, with selected coverage in Lane A.

### Verification Requirements

- `REQ-ODD-WORLD-MODEL-BUILD-VERIFY-001`
- `REQ-ODD-WORLD-MODEL-BUILD-VERIFY-002`
- `REQ-ODD-WORLD-MODEL-BUILD-VERIFY-003`
- `REQ-ODD-WORLD-MODEL-BUILD-VERIFY-004`

These are qualified across all three lanes:

- Lane A for deterministic intermediate proof surfaces
- Lane B for retained steel-thread proof
- Lane C for installed-product/live scenario proof

---

## 6. Planned Test Surface Shape

The downstream governed test surfaces should be:

- `build_tenants/python/test_env/40-generated-test-stack.md`
- `build_tenants/python/test_env/tests/40-generated-test-modules.md`
- realized Python test source under `build_tenants/python/test_env/tests/`
- `specification/scenarios/30-generated-testcase-authority.md`
- `build_tenants/python/test_env/50-generated-run-archive.md`

The realized Python test source should include at least:

- carrier/runtime tests
- semantic steel-thread tests
- installed sandbox tests

Each realized test module must declare `Validates:` coverage for the requirements it claims.

---

## 7. Evidence And Run Archive Expectations

The run archive must preserve enough evidence to answer:

- which lane ran
- which commands executed
- which reports or summaries were produced
- whether the retained trade/APRA proving corpus converged
- whether the installed sandbox proof converged
- what failures, if any, remain open

The run archive is therefore not only a pass/fail note. It is the governed evidence projection for the current qualified line.

---

## 8. Immediate Downstream Use

This test design is the authority for:

- selecting the current Python test stack
- partitioning governed test modules
- generating testcase authority
- generating the first run archive and qualifying the retained test line
