# Generated Implementation Stack Profile

This implementation stack profile is regenerated by the bounded odd_sdlc constructor turn.

**Project**: odd_world_model
**Platform**: Python / ODD_METHOD / GTL + ABG
**Generated**: 2026-04-16
**Derived from**: `build_tenants/python/design/40-generated-implementation-design.md`
**Status**: Active Python-first implementation stack for the current live tenant

---

## 1. Platform Baseline

| Dimension | Selection | Rationale |
|---|---|---|
| Language | Python 3.11+ | Matches the live package, installed runtime usage, and current proving environment |
| Product-authoring method | ODD_METHOD + SPEC_METHOD | Governs the software-product and GTL carrier shape |
| Runtime substrate | GTL + ABG via installed `.genesis/` and local `odd_world_model` package | Matches the current odd_world_model carrier and odd_sdlc dogfood line |
| Package root | `build_tenants/python/code/odd_world_model` | Current governed code root for the live tenant |
| Execution mode | `python -m odd_world_model ...` and retained module entry points | Matches the shipped CLI/operator surfaces |
| Realization mode | `selected_output_tree` | Matches normalized project constraints and installed odd_sdlc analysis |

---

## 2. Selected Stack

### 2.1 Core Implementation Stack

- Python package implementation under `build_tenants/python/code/odd_world_model`
- GTL carrier published in `odd_world_model.gtl_module`
- ABG/runtime interaction through installed `.genesis`
- deterministic `F_D` evaluators and checks in Python
- retained steel-thread build modules in Python

### 2.2 Query, Proof, And Install Stack

- query lane: Python filesystem-first traversal
- proof lane: Python conventional + covariant proof projection
- release/install lane: Python installer under `odd_world_model.release.install`

This keeps the first live product line in one realization stack and avoids
premature multi-language or service-boundary drift.

---

## 3. Build And Test Tooling

| Concern | Current Selection | Notes |
|---|---|---|
| Module execution | `python -m <module>` | Current operator surface for build/query/proof/install |
| Syntax/compile sanity | `python -m py_compile` | Used in the current proving line |
| Test runner | `pytest` | Current non-live test convention across the line |
| Package resolution | `PYTHONPATH=.genesis:build_tenants/python/code` (source) or `.genesis/odd_world_model/python/code` (installed) | Matches current runtime contracts |
| Workspace governance | installed `odd_sdlc` over mutable `odd_world_model` source project | Current dogfood boundary |

The stack profile resolves the blank normalized project-constraint fields in
practice:

- language: Python
- test runner: pytest

Those should be treated as the effective current stack even if the starter
constraints file has not yet been deepened to name them explicitly.

---

## 4. Dependency Posture

### 4.1 Core Dependencies

- installed `.genesis` runtime providing GTL/ABG execution support
- local `odd_world_model` Python package

### 4.2 Current Library Posture

- standard-library-first implementation where possible
- existing runtime/library dependencies inherited through the installed GTL/ABG
  substrate
- no parallel JavaScript, TypeScript, JVM, or database-native core build path
  in the first live line

This is intentionally conservative. The first task is to make the current
Python line fully governed and replayable before widening the stack.

---

## 5. Runtime And Artifact Formats

| Artifact Class | Current Format |
|---|---|
| generated SDLC surfaces | Markdown |
| source observation / trace / assurance / query summaries | JSON |
| published domain artifacts and world-model outputs | JSON directories/files |
| proof outputs | Markdown + SQL + JSON |

The stack therefore favors:

- Python for orchestration and materialization
- Markdown for human-governed software-product surfaces
- JSON for semantic and runtime-carrier surfaces

---

## 6. Explicit Non-Selections

The current stack does **not** select:

- a database-backed query plane as the primary carrier
- a UI/frontend runtime as part of the core build line
- a second implementation language for the first live tenant
- container orchestration or cloud deployment as part of the current proving
  requirement

Those may appear later as downstream projections or release/runtime surfaces,
but they are not part of the current implementation stack selection.

---

## 7. Stack Constraints

### 7.1 Must Hold

- Python remains the authoritative first live implementation stack
- GTL graph functions remain the constructive carrier
- deterministic `F_D` checks remain bound into the same Python line
- installed-product execution stays distinct from mutable source-workspace
  authoring

### 7.2 Must Not Happen

- no reintroduction of deleted compatibility wrappers as stack indirection
- no manual mirroring into installed trees instead of running install
- no query-serving layer promoted to constitutional source of truth

---

## 8. Downstream Implications

This stack profile drives the next generated surfaces:

- implementation modules should be described in Python package terms
- code-surface qualification should target `build_tenants/python/`
- test design and test modules should assume `pytest` and Python entry points
- release surfaces should assume Python-installed product boundaries

That is sufficient to keep the remaining dogfood line on one clear technical
stack without reopening the architectural choice at each edge.
